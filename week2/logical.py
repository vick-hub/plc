import sys
import os


def main():
    # comparisons: ==, !=, <, <=, >, >=, is, is not
    # logical operators: and, or, not
    # +=, -=
    # v1 <= x <= v2
    # the random library
    return os.EX_OK


if __name__ == "__main__":
    sys.exit(main())
